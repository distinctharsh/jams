USE jams_db;

CREATE TABLE audit_trail (
    audit_id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT UNSIGNED NULL,
    login_name VARCHAR(100) NULL,
    action VARCHAR(50) NOT NULL,
    action_description VARCHAR(255) NULL,
    ip_address VARCHAR(45) NULL,
    user_agent VARCHAR(500) NULL,
    login_time DATETIME NULL,
    logout_time DATETIME NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_login_name (login_name),
    INDEX idx_action (action),
    INDEX idx_created_at (created_at),
    INDEX idx_ip_address (ip_address)
);

CREATE TABLE audit_action (
    action_id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT UNSIGNED NULL,
    login_name VARCHAR(100) NULL,
    module VARCHAR(100) NULL,
    action VARCHAR(100) NOT NULL,
    record_id VARCHAR(100) NULL,
    description VARCHAR(500) NULL,
    ip_address VARCHAR(45) NULL,
    user_agent VARCHAR(500) NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id),
    INDEX idx_login_name (login_name),
    INDEX idx_module (module),
    INDEX idx_action (action),
    INDEX idx_record_id (record_id),
    INDEX idx_created_at (created_at),
    INDEX idx_ip_address (ip_address)
);


ALTER TABLE `user`
ADD COLUMN `must_change_password` TINYINT NOT NULL DEFAULT 0 AFTER `hash`,
ADD COLUMN `password_changed_at` DATETIME NULL AFTER `must_change_password`;


==========02092026=======================================================

ALTER TABLE `user`
	CHANGE COLUMN `password_reset_req` `password_reset_req` INT NOT NULL DEFAULT '1' AFTER `ugc_id`;

ALTER TABLE `user`
ADD COLUMN `is_locked` TINYINT(1) NOT NULL DEFAULT 0 AFTER `isactive`,
ADD COLUMN `mfa_required` TINYINT(1) NOT NULL DEFAULT 1 AFTER `is_locked`;